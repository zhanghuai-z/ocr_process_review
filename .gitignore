# Build outputs
build/
dist/
# Keep root .spec files (needed for PyInstaller build)
# *.spec  — not ignoring, ocr_process.spec is tracked

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.eggs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project cache (created at runtime)
*.ocrproj
.cache/
_view/
debug/
*.layout-api.json
*.layout-api-raw.png
*.layout-app-overlay.png
.state/

# Local archive/quarantine. Files here are intentionally kept recoverable
# but are not part of the active project surface.
null/

# Archives (large sample files)
*.rar

# Environment
.env
*.token
