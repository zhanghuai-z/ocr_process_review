请全面接手此项目的 UI 改造，一次完成以下所有内容。
完成后提交到 `claude/editable-canvas` 分支，留 handoff 说明。

你可以改任意文件，不限 UI。引擎侧核心逻辑也可以动。

---

## 0. 最高优先级：解决 bbox 框偏移

四个 AIStudio 模型的版面分析结果在画布上显示时框都是偏移的。
这是**最核心的问题**，其他地方做得再好，框不对就没用。

用以下手段排查：
1. 调 API 看原始 JSON 响应，参考 `scripts/` 目录的官方示例和 `*.layout-api.json`
2. 理解每个模型返回坐标的坐标系（百分比？模型 canvas？像素？）
3. 确保坐标转换到 `page.display_image_path` 的像素空间正确
4. 对比 `*.layout-api-raw.png`（原始 bbox）和 `*.layout-app-overlay.png`（处理后 bbox），定位问题在哪一层

不要在 `_rescale_blocks_if_suspicious` 里打补丁，在坐标提取阶段就做对。

## 1. 主界面布局

- 画布占主体空间，尽可能大
- 按钮精简，能用图标代替的用图标（QToolBar 或 QPushButton + icon）
- 减少占位，工具类按钮不要占一整行

## 2. 横校改造

当前是左图右文，翻行校对。改为：
- **上图下文**：上方显示当前行的图片裁切，下方显示可编辑文本
- **相邻行上下文**：当前行上下各显示 1-2 行作为上下文参考（灰色或半透明），当前行高亮
- **不要切分成多张小图翻页**，而是连续滚动或单行切换
- 保留低置信字符红底高亮
- 键盘快捷键：上下键切换行，Enter 确认

## 3. 纵校改造

当前是按 block 切图，改为四种视图组合在一个画布里：

| 区域 | 内容 |
|------|------|
| 左上 | **单字列表** — 当前页所有 OCR 识别出的独立字符，按阅读顺序排列 |
| 右上 | **相同字图像集合** — 从多页中提取同一个字符的切图，排成缩略图网格 |
| 左下 | **OCR 文本** — 当前页完整文本，可编辑 |
| 右下 | **原图** — 带框叠加显示 |

**关键交互**：
- 点击单字列表中的某个字 → OCR 文本中高亮该字 + 原图上定位该字位置
- OCR 文本中修改文字 → 同步更新到单字列表
- 相同字图像集合用于对比同一个字在不同页的识别情况

纵校的目的是发现"同一个字在不同页面被识别成不同结果"的问题，
以及快速定位和修正识别错误的字。

## 4. 画布

- 可编辑框已在前面轮次初步实现，继续完善
- 滚轮缩放（已做）
- 框可选中、拖拽、显示坐标信息

## 5. 其他

- 现有功能和数据流不能破坏
- `python tests/test_core.py` 必须通过
