# OCR Process 新功能基线说明（2026-06-12）

## 基线结论

当前 `coord/phase1-stabilization` 已作为新功能开发前的稳定候选基线收口。

本基线目标不是“架构完全干净”，而是保证主流程可继续开发新功能：

- 导入、版面分析、OCR、横校、纵校、保存重开、导出主链不被已知 P0/P1 阻断。
- SQLite rowid 不再作为跨模块稳定身份的唯一依据，稳定 UID 已覆盖存储、导出、proof event、char proof entry、operation log。
- 文本字段契约集中到 `Line.ensure_text_contract()`，避免 `ocr_text` 被人工终稿反向污染。
- OCR 入口 gate 已区分错误来源：OCR-owned 错误可重新进入 OCR，非 OCR/上游错误阻断提交。
- 旧兼容入口保留但不再作为新功能设计依据。

## 新功能前已收口

1. 稳定业务 UID
   - Page/Block/Line/Char 均有稳定 `uid`。
   - 保存时优先用 UID 恢复实体，避免 stale rowid 覆盖或跨 project 污染。
   - 导出 source、proof event、operation log 已优先携带 UID。

2. OCR 状态边界
   - 项目级状态拆为 `has_any_ocr_result`、`all_pages_ocr_done`、`has_any_ocr_done_page`。
   - OCR gate 对 OCR-owned 错误返回 `page_state="ocr_error"`，允许重新进入 OCR；对非 OCR/上游错误返回 `page_state="error"`，不允许继续提交。

3. 文本字段边界
   - `final_text` 表示人工最终文本。
   - `ocr_text` 表示 OCR 原始观察，不用 `final_text` 回填。
   - UI/导出主路径应读取 `display_text` 或 `final_text`，新功能不要直接把 `line.text` 当当前文本。

4. Proof 同步
   - 横校/纵校事件统一通过 `proof_request_matches_page()` 和 `proof_request_matches_line()` 匹配。
   - UID 优先，rowid 只作为旧 payload 兜底。

## 新功能开发约束

- 新代码不要新增依赖 SQLite rowid 的业务引用。
- 新代码不要直接写 `line.text` 作为人工编辑结果，人工结果写 `line.final_text` 或调用 `line.update_final_text()`。
- 新代码读取展示文本时使用 `line.display_text`。
- 新代码涉及页面流程时优先使用 `page_gate_info()` 和 `PageStatus`，不要自行用 `len(lines)` 推导 OCR 完成。
- 新代码不要扩展 legacy payload；如必须兼容旧项目，把兼容逻辑隔离在转换层。

## 延期技术债

这些不作为新功能前阻断，后续逐步处理：

- `PageStatus` 还不是完整状态机，`LAYOUT_CONFIRMED`、`PROOFING`、`PROOF_DONE` 等状态需要后续设计去留。
- `Line.text/final_text/ocr_text/original_text` 仍是兼容期模型，长期应拆为 OCR observation、proof edit state、display view model。
- `ProofUpdateRequest.from_legacy()`、`to_legacy_payload()` 和 `ProofStateBus` legacy dispatch 仍保留，用于旧调用兼容。
- `layout_analyzer.py` 和 `ocr_pipeline.py` 仍承担较多职责，后续可拆配置、HTTP 调用、响应解析、领域转换。
- `v_proof` 字符索引仍是全量重建，当前靠 debounce 缓解，大项目性能后续再优化。
- Hanwang 版面分析 `_hanwang_analyze()` 目前不是默认主链；当前主链是 Paddle/VL 版面分析 + Hanwang OCR。若后续要启用 Hanwang layout，需要单独立项确认。

## 人工基线测试清单

进入新功能前建议人工跑一轮：

- 导入真实样例项目。
- 运行版面分析。
- 手动画框：新增、删除、改属性、锁定文本框、行内公式覆盖。
- 运行 OCR。
- 横校修改一行并保存。
- 纵校修改一个字符并保存。
- 关闭项目并重开，确认文本、框、UID 相关行为稳定。
- 导出 JSON/XML，确认 `final_text` 与 `ocr_text` 字段符合预期。

## 后续原则

从本基线开始，新功能优先，不再让 P2 架构债阻塞功能推进。只有崩溃、保存丢数据、主流程不可用、OCR 路由明显错误进入阻断队列。
